import java.util.*;
public class Main
{
	public static void main(String[] args) {
		Scanner obj=new Scanner(System.in);
		int n=obj.nextInt();
		int m=obj.nextInt();
		for(int i=n;i<=m;i++)
		{
		    int act=i;
		    int num=i;
		    int rev=0;
		    while(num!=0)
		    {
		        int rem=num%10;
		        rev=rev*10+rem;
		        num=num/10;
		    }
		    
		    if(act==rev)
		    System.out.println(act);
		}
	}
}
